apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cicd-appset
  namespace: argocd
  labels:
    app.kubernetes.io/name: cicd-appset
    app.kubernetes.io/part-of: bootstrap
spec:
  project: cicd

  source:
    repoURL: https://github.com/bitsdalab/deployment.git
    targetRevision: feature/ext-secrets
    path: argocd/appsets/cicd
    
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
